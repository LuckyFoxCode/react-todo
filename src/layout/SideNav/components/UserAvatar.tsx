import { Box } from '@/components/common';

export const UserAvatar: React.FC = () => {
  return (
    <Box className='absolute -top-[40px] left-1/2 -translate-x-1/2 w-20 h-20 rounded-full bg-white' />
  );
};
